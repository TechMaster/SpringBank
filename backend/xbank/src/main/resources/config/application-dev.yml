logging:
  level:
    ROOT: DEBUG
    io.github.jhipster: DEBUG
    com.xbank: DEBUG

spring:
  profiles:
    active: dev
    include:
      - swagger
#      - tls

  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false # we use Webpack dev server + BrowserSync for livereload
  jackson:
    serialization:
      indent-output: true
  h2:
    console:
      enabled: false
  liquibase:
    # Remove 'faker' if you do not want the sample data to be loaded automatically
    contexts: dev, faker
    url: jdbc:postgresql://13.229.132.148:5432/xbank
  mail:
    host: localhost
    port: 25
    username:
    password:
  messages:
    cache-duration: PT1S # 1 second, see the ISO 8601 standard
  r2dbc:
    url: r2dbc:postgresql://13.229.132.148:5432/xbank
    username: root
    password: xbank@123
  thymeleaf:
    cache: false
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/auth/realms/microservice

server:
  port: 8081
